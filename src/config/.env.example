SLACK_WEBHOOK_URL=
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=bitcoin_brute
DB_SSLMODE=disable  # SSL mode options: disable, prefer, require, verify-ca, verify-full

# Telegram notifications (optional)
# To enable, create a bot with @BotFather and get the token
# Then start a chat with your bot and get the chat ID
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=  # For supergroup chats, use format: -100{chat_id}
TELEGRAM_STATS_INTERVAL=15  # Minutes between stats updates